title: Faith Frontier Ecclesiastical Trust
description: A public documentation portal uniting law, faith, and transparency — featuring New Jersey case records, public-trust analysis, and moral commentary.

url: "https://faithfrontier.org"   # your real domain
baseurl: ""                        # leave empty for custom domains

collections:
  cases:
    output: true
    permalink: /cases/:slug/
  essays:
    output: true
    permalink: /faith/:slug/

# optional helpful defaults
markdown: kramdown
permalink: /:title/
timezone: America/New_York

defaults:
  - scope:
      path: ""
      type: "cases"
    values:
      layout: case
      status: Active
  - scope:
      path: ""
      type: "essays"
    values:
      layout: essay
      section: faith
markdown: kramdown
theme: minima

